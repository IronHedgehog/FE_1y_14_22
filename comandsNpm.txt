console.log("Привіт, мене запустила node в терміналі!)";

const arr = [1, 2, 3, 4, 5];

for (const number of arr) {
  console.log(number + number);
}

// pwd - Показує поточний шлях(ваше місцезнаходження)
//  ls - показує всі файли в папці
// ls -la - можемо побачити всі файли разом з прихованими
// cd - дозволяє навігуватись по папкам
// clear - очищує термінал
// touch - команда яка дозволяє створити файл
//mkdir - створення папкию
// mv - Переіменувати файл
//  rm - дозволяє видаляти файли
// mv - переміщення файлу  *fileName* folderName
